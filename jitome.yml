# Jitome is a simple file watcher. - https://github.com/kohkimakimoto/jitome

init:
    script: |
      echo "booted!"


# targets.
build:
  notification: true
  watch:
    - base: ""
      ignore: [".git"]
      pattern: "*.go"
  script: |
    make
