build:
  watch:
    - base: ""
      ignore_dir: [".git"]
      pattern: "*.go"
    - base: ""
      ignore_dir: [".git"]
      pattern: "*.go"
  script: |
    echo $JITOME_FILE
    echo "hogehoge"
    ls -la

test:
  watch:
    - base: ""
      ignore_dir: [".git"]
      pattern: "*.go"
  script: |
    echo "test"
