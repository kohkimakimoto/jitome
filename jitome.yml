# Jitome is a simple file watcher. - https://github.com/kohkimakimoto/jitome

init:
    script: |
      echo "booted!"

      
# targets.
build:
  watch:
    - base: ""
      ignore: [".git"]
      pattern: "*.go"
  script: |
    go build .
